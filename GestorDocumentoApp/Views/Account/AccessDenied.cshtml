@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="mx-auto mt-12 max-w-xl rounded-lg bg-white p-8 shadow">
    <h1 class="text-2xl font-semibold text-gray-900">Acceso denegado</h1>
    <p class="mt-3 text-gray-700">
        No tienes permisos para acceder a este recurso.
    </p>
    <div class="mt-6">
        <a asp-controller="Home" asp-action="Index" class="rounded bg-blue-600 px-4 py-2 text-white hover:bg-blue-700">
            Volver al inicio
        </a>
    </div>
</div>
